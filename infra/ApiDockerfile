FROM eclipse-temurin:21.0.2_13-jre

# 작업 디렉토리 설정
WORKDIR /app

# JAR 파일 복사 (빌드 컨텍스트 내에 있어야 함)
COPY github_action.jar .

# 애플리케이션 실행 명령어
CMD ["java", "-jar", "github_action.jar", "--spring.profiles.active=local-docker"]
